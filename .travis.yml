language: python
sudo: false
branches:
  only:
    - master
python:
  - "3.5"
  - "3.6"
  - "3.7"
  
install:
  - pip install --upgrade setuptools
  - pip install -r requirements.txt

script: 
  - py.test difi --cov=difi

after_success:
  - coveralls