language: python
sudo: false
branches:
  only:
    - master
python:
  - "2.7"
  - "3.6"
  
install:
  - pip install --upgrade setuptools
  - pip install -r requirements.txt

script: 
  - py.test difi --cov=difi

after_success:
  - coveralls
